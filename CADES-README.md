# CADES/Baseline Notes

`CADES/Baseline` support is deprecated for AutoPF, so the old CADES-specific
module, conda environment, and submit-script instructions have been removed.

Use the portable setup and examples in `README.md` instead, and adapt the
MatEnsemble launch commands to the currently supported HPC environment.
